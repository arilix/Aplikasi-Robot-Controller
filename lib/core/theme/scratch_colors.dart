import 'package:flutter/material.dart';

class ScratchColors {
  // Motion (Movement) - Blue
  static const motion = Color(0xFF4C97FF);
  static const motionDark = Color(0xFF3373CC);
  static const motionLight = Color(0xFF7CB5FF);

  // Events (Actions - Kick) - Yellow
  static const events = Color(0xFFFFAB19);
  static const eventsDark = Color(0xFFCF8B17);
  static const eventsLight = Color(0xFFFFBD4A);

  // Looks (Special - Boost/Attack) - Purple
  static const looks = Color(0xFF9966FF);
  static const looksDark = Color(0xFF774DCB);
  static const looksLight = Color(0xFFB088FF);

  // Operators (Pro Settings) - Green
  static const operators = Color(0xFF59C059);
  static const operatorsDark = Color(0xFF389438);
  static const operatorsLight = Color(0xFF7DD97D);

  // Control (Connection/Status) - Orange
  static const control = Color(0xFFFFBF00);
  static const controlDark = Color(0xFFCC9900);
  static const controlLight = Color(0xFFFFD633);

  // Danger (Stop) - Red
  static const danger = Color(0xFFFF6680);
  static const dangerDark = Color(0xFFE63956);
  static const dangerLight = Color(0xFFFF8CA3);

  // Background & Surface
  static const background = Color(0xFFF9F9F9);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFFE8E8E8);

  // Text
  static const textPrimary = Color(0xFF2D3748);
  static const textSecondary = Color(0xFF718096);
  static const textLight = Color(0xFFA0AEC0);
  static const textWhite = Color(0xFFFFFFFF);

  // Status
  static const success = Color(0xFF48BB78);
  static const warning = Color(0xFFED8936);
  static const info = Color(0xFF4299E1);
}
